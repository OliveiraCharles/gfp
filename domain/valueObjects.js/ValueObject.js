export class ValueObject {
    // if (this.constructor === ValueObject) {
    //     throw new Error("Não pode instanciar classe abstrata!");
    // }
    constructor(id, nome, descricao) {
      this.id = id;
      this.nome = nome;
      this.descricao = descricao;
    }

    getName(id){
        
        return this.name
    }
    // Motivos Para Mudança

  }
  